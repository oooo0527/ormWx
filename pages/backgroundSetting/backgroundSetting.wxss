.container {
  padding: 20rpx;
  min-height: 100vh;
  box-sizing: border-box;
  background: #d9dde0;
}

.header {
  background: #ffffff;
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 4rpx 20rpx rgba(17, 17, 17, 0.1);
  box-sizing: border-box;
  border: 2rpx solid #869dae;
}

.page-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #111111;
  text-align: center;
}

.settings-grid {
  display: flex;
  flex-direction: column;
  gap: 40rpx;
  box-sizing: border-box;
}

.setting-item {
  background: #ffffff;
  border-radius: 20rpx;
  padding: 30rpx;
  box-shadow: 0 4rpx 20rpx rgba(17, 17, 17, 0.1);
  box-sizing: border-box;
  border: 2rpx solid #869dae;
}

.item-label {
  font-size: 32rpx;
  color: #111111;
  font-weight: bold;
  margin-bottom: 30rpx;
  display: block;
}

/* 颜色选择器 */
.color-palette {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
  box-sizing: border-box;
}

.color-item {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  box-shadow: 0 2rpx 8rpx rgba(17, 17, 17, 0.1);
  box-sizing: border-box;
  border: 2rpx solid #d9dde0;
}

.color-item:active {
  transform: scale(0.9);
}

/* 渐变选项 */
.gradient-options {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  box-sizing: border-box;
}

.gradient-btn {
  padding: 25rpx;
  border-radius: 15rpx;
  color: #111111;
  font-weight: bold;
  text-align: center;
  box-shadow: 0 2rpx 8rpx rgba(17, 17, 17, 0.1);
  box-sizing: border-box;
  font-size: 28rpx;
  border: 2rpx solid #869dae;
}

.gradient-btn:active {
  transform: scale(0.98);
}

/* 自定义选项 */
.custom-options {
  display: flex;
  gap: 20rpx;
  box-sizing: border-box;
}

.option-btn {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 25rpx;
  border-radius: 15rpx;
  border: none;
  background: #f8f9fa;
  color: #111111;
  font-size: 28rpx;
  box-sizing: border-box;
  border: 2rpx solid #869dae;
}

.upload-btn {
  background: linear-gradient(135deg, #111111 0%, #869dae 100%);
  color: white;
}

.reset-btn {
  background: linear-gradient(135deg, #869dae 0%, #d9dde0 100%);
  color: #111111;
}

.option-btn:active {
  transform: scale(0.98);
}

.btn-icon {
  width: 45rpx;
  height: 45rpx;
  margin-bottom: 15rpx;
}

/* 响应式设计 */
@media (max-width: 768px) {
  .container {
    padding: 15rpx;
  }

  .header {
    padding: 25rpx;
  }

  .page-title {
    font-size: 32rpx;
  }

  .setting-item {
    padding: 25rpx;
  }

  .item-label {
    font-size: 28rpx;
    margin-bottom: 25rpx;
  }

  .color-item {
    width: 50rpx;
    height: 50rpx;
  }

  .gradient-btn {
    padding: 20rpx;
    font-size: 26rpx;
  }

  .option-btn {
    padding: 20rpx;
    font-size: 26rpx;
  }

  .btn-icon {
    width: 40rpx;
    height: 40rpx;
  }
}