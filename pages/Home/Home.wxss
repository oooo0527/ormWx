.container {
  /* /* background: linear-gradient(180deg, #E9E8EF 0%, #f5f5f5 100%); */
  /* background: #1E2020; */
  background: linear-gradient(0deg, #000000 0%, #f5f5f5 40%, #f5f5f5 100%);
  /* height: 100vh;
  overflow: hidden;
  overflow-y: scroll; */
  overflow: hidden;
}

/* 添加悬挂灯泡样式 */
.hanging-lamp {
  position: fixed;
  top: 0;
  right: 30rpx;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 20rpx;
}

.lamp-rope {
  width: 2rpx;
  height: 180rpx;
  background: gold;
  box-shadow: 0 0 2rpx 1rpx rgba(255, 215, 0, 0.5);
}

.lamp-bulb {
  width: 60rpx;
  height: 60rpx;
  /* background: #cccccc; */
  /* border-radius: 50%; */
  position: relative;
  /* box-shadow: 0 0 10rpx rgba(0, 0, 0, 0.3); */
  transition: all 0.3s ease;
}




/* 弹窗样式 */
.lamp-popup {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 999;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.lamp-popup.show {
  opacity: 1;
  visibility: visible;
}

.popup-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
}

.popup-content {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);

  width: 90%;
  /* width: 100%; */
  height: 70vh;
  background: #ffffff;
  border-radius: 20rpx;
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.2);
  /* transform: translateY(-20rpx); */
  transition: transform 0.3s ease;
  overflow: hidden;
  overflow-y: scroll;
  z-index: 999;
}



.popup-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 30rpx;
  background: #000000;
  border-bottom: 1rpx solid #e0e0e0;
  color: #ffffff;
}

.popup-title {
  font-size: 32rpx;
  font-weight: bold;
  /* color: #333333; */
}

.close-btn {
  font-size: 40rpx;
  color: #999999;
  padding: 10rpx;
  cursor: pointer;
}

.popup-body {
  padding: 20rpx 30rpx;
  max-height: 60vh;
}

.event-item {
  padding: 20rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}

.event-item:last-child {
  border-bottom: none;
}

.event-date {
  font-size: 24rpx;
  color: #999999;
  margin-bottom: 10rpx;
}

.event-title {
  font-size: 30rpx;
  font-weight: bold;
  color: #333333;
  margin-bottom: 10rpx;
}

.event-desc {
  font-size: 26rpx;
  color: #666666;
  line-height: 1.4;
}

.no-events {
  text-align: center;
  padding: 40rpx 0;
  color: #999999;
  font-size: 28rpx;
}

/* 新设计的特色行程表样式 */
.event-calendar-header {
  text-align: center;
  padding: 30rpx 0;
  position: relative;
  margin-bottom: 20rpx;
}

.calendar-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333333;
  display: block;
  margin-bottom: 10rpx;
}

.calendar-subtitle {
  font-size: 24rpx;
  color: #999999;
  letter-spacing: 2rpx;
}

.event-list-container {
  padding: 0 30rpx 30rpx;
  position: relative;
  z-index: 2;
}

.event-card {
  background: linear-gradient(135deg, #ffffff 0%, #f8f8f8 100%);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 10rpx 20rpx rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(0, 0, 0, 0.03);
  display: flex;
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}

.event-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 6rpx;
  height: 100%;
  background: linear-gradient(to bottom, #ff9a9e, #fad0c4);
}

.event-card:hover {
  transform: translateY(-5rpx);
  box-shadow: 0 15rpx 30rpx rgba(0, 0, 0, 0.1);
}

.event-date-badge {
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(135deg, #030303 0%, #764ba2 100%);
  border-radius: 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin-right: 30rpx;
  box-shadow: 0 8rpx 16rpx rgba(0, 0, 0, 0.1);
  position: relative;
  overflow: hidden;
  color: #ffffff;
}

.event-date-badge::after {
  content: '';
  position: absolute;
  top: -10rpx;
  right: -10rpx;
  width: 30rpx;
  height: 30rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
}

.date-day {
  font-size: 36rpx;
  font-weight: bold;
  color: #ffffff;
  line-height: 1;
}

.date-month {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.9);
  text-transform: uppercase;
  letter-spacing: 1rpx;
  margin-top: 6rpx;
}

.event-content {
  flex: 1;
}

.event-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333333;
  margin-bottom: 15rpx;
  line-height: 1.3;
}

.event-desc {
  font-size: 26rpx;
  color: #666666;
  line-height: 1.5;
  margin-bottom: 20rpx;
}

.event-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.event-time {
  font-size: 24rpx;
  color: #999999;
  background: rgba(0, 0, 0, 0.03);
  padding: 6rpx 16rpx;
  border-radius: 30rpx;
}

.event-decoration {
  position: absolute;
  right: 20rpx;
  bottom: 20rpx;
}

.decoration-line {
  width: 30rpx;
  height: 2rpx;
  background: linear-gradient(to right, transparent, #cccccc);
  margin-bottom: 8rpx;
}

.decoration-dot {
  width: 8rpx;
  height: 8rpx;
  background: #cccccc;
  border-radius: 50%;
  margin-left: 22rpx;
}

.no-events-card {
  text-align: center;
  padding: 80rpx 30rpx;
  background: linear-gradient(135deg, #ffffff 0%, #f8f8f8 100%);
  border-radius: 20rpx;
  box-shadow: 0 10rpx 20rpx rgba(0, 0, 0, 0.05);
}

.empty-icon {
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 30rpx;
  opacity: 0.5;
}

.no-events-text {
  display: block;
  font-size: 32rpx;
  color: #333333;
  margin-bottom: 15rpx;
  font-weight: 500;
}

.no-events-subtext {
  font-size: 26rpx;
  color: #999999;
}

.calendar-decoration {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 1;
}

.decoration-circle {
  position: absolute;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
}

.circle-1 {
  width: 200rpx;
  height: 200rpx;
  top: 10%;
  right: 10%;
}

.circle-2 {
  width: 150rpx;
  height: 150rpx;
  bottom: 20%;
  left: 5%;
}

.circle-3 {
  width: 100rpx;
  height: 100rpx;
  top: 40%;
  left: 15%;
}

.conten-box-bg {
  background: linear-gradient(0deg, #000000 0%, #f5f5f5 40%, #f5f5f5 100%);
  min-height: calc(100vh - 690rpx);
  overflow: hidden;
  overflow-y: scroll;

  z-index: 99;
  position: relative;
  width: 100%;
  border-top-left-radius: 50rpx;
  border-top-right-radius: 50rpx;
  margin: 0 auto;
  margin-top: -50px;
}


.banner {
  width: 100vw;
  height: 800rpx;
  display: flex;
  flex-direction: column;
  /* background: rgba(0, 0, 0, 0.209);
  border-bottom-left-radius: 50rpx;
  border-bottom-right-radius: 50rp;x; */
  /* margin-bottom: 40rpx; */
  /* opacity: 0.5; */
}

.banner-swiper {
  width: 100%;

  /* height: 800rpx; */
  height: 100%;
}

.banner-image {
  width: 100%;
}


.menu-scroll-view {
  position: relative;
  /* margin-top: -100rpx; */


}

.menu-container {
  display: flex;

  padding: 0 30rpx;
  position: relative;
  /* margin-top: -100rpx; */
  width: 100%;
  /* margin-left: -10%; */
}

.menu-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #666666;
  /* border: 4rpx solid #E9E8EF; */
  height: 250rpx;
  margin: 0 10rpx;
  border-radius: 16rpx;
  position: relative;
  width: 180rpx;
  overflow: hidden;

}

.menu-item:nth-of-type(2n) {
  margin-top: 18rpx;
  box-shadow: 0 2rpx 4rpx #E9E8EF;
  animation-delay: 0.5s;
  animation: floatUp 3s ease-in-out infinite;
}

.menu-item:nth-of-type(2n+1) {
  margin-bottom: 18rpx;
  box-shadow: 0 4rpx 6rpx 2rpx #0c1117;
  animation-delay: 1s;
  animation: floatDown 3s ease-in-out infinite;
}

/* 上下浮动动画 */
@keyframes floatUp {
  0% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-10rpx);
  }

  100% {
    transform: translateY(0);
  }
}

/* 上下浮动动画 */
@keyframes floatDown {
  0% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(10rpx);
  }

  100% {
    transform: translateY(0);
  }
}

.spacer {
  width: 100%;
  height: 100rpx;
  color: #E9E8EF;
  padding: 0 30rpx;
}

.movable-area {
  width: 100%;
  height: 100rpx;
}


.menu-icon {
  border-radius: 16rpx;
  width: 100%;
  height: 250rpx;
  border-radius: 16rpx;
}

.menu-text {
  position: absolute;
  font-size: 18rpx;
  color: #ffffff;
  background-color: #000000;
  bottom: 0;
  /* border-radius: 8rpx; */
  width: 100%;
  text-align: center;
}

.contentList {
  /* padding: 10rpx 30rpx; */
  width: 100%;
  padding: 40rpx;
}


.ListBox {
  display: flex;
  height: 400rpx;
  justify-content: space-between;
  color: #333333;
  text-align: left;


}

.list-image {
  width: 90%;
  /* height: 100%; */

  border-radius: 16rpx;
  position: relative;

}

.list-image::after {
  content: '她不完美!                            但是是我的榜样';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 80rpx;
  font-size: 24rpx;
  background: #2a2929;
  opacity: 0.7;
  padding: 10rpx;
}

.list-item-text {
  line-height: 60rpx;
  position: absolute;
  top: 30rpx;
  color: #333333;
  z-index: 2;
}

.list-item {
  position: relative;
  width: 48%;
  /* border: 4rpx solid black; */
  /* padding: 20rpx; */
  /* background: black; */
  /* border: 4rpx solid #E9E8EF; */
  border-radius: 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx 0;
  /* background: #E9E8EF; */
  box-shadow: 0 0 15rpx #333333;
  /* padding: 0 20rpx; */
  /* padding-bottom: 80rpx; */

}



.list-item1 {
  margin-left: 20rpx;
  flex: 1;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.music-item {
  height: 30%;
  /* border: 4rpx solid #E9E8EF; */
  border-radius: 16rpx;
  text-align: left;
  padding-left: 40rpx;
  /* background: #f48eb5; */
}

.music-title {
  font-size: 28rpx;
  color: #666666;
  line-height: 50rpx;
}

.music-des {
  font-size: 24rpx;
  color: #333333;
  line-height: 30rpx;
}


.music-item:nth-of-type(2n) {
  background: #1E2020;

  /* .music-title {
    font-size: 28rpx;
    color: #f9f6f6;
    line-height: 50rpx;
  }

  .music-des {
    font-size: 24rpx;
    color: #f5f2f2;
    line-height: 30rpx;
  } */

}

.music-item:nth-of-type(2n) .music-title,
.music-item:nth-of-type(2n) .music-des {
  color: #f9f6f6;
}

.music-item:nth-of-type(2n+1) {
  background: linear-gradient(90deg, #eeeeee 0%, #f5f5f5 100%);
  /* box-shadow: 0 0 4rpx #333333; */
  /* background: linear-gradient(90deg, #468fa9 0%, #f5f5f5 100%); */
}

/* //品牌 */

.content {
  width: 100%;
  padding: 20rpx;
  background: #000000;
  /* background: #f5dddd; */
  /* border-radius: 24rpx; */
  /* margin: 0 auto; */
  color: #f7f4f4;
  margin: 0 auto;
  box-shadow: 0 0 15rpx #dddddd;
  margin-bottom: 40rpx;

  /* margin-top: 80rpx; */
  /* width: 100vw; */


}

.content-header {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
}

.content-container {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
}

.content-container-item {
  /* width: 20%; */
  border-radius: 100rpx;
  height: 100rpx;
  border: 4rpx solid black;
  /* box-shadow: 0 0 15rpx #f48eb5; */
  /* flex: 1; */
  /* margin: 0 10rpx; */
  position: relative;



}

.content-image {
  width: 100rpx;
  height: 100rpx;
  border-radius: 100rpx;
  opacity: 0.9;

}

.content-text {
  text-align: center;
  /* margin-top: -80rpx; */
  height: 50rpx;
  width: 50rpx;
  position: absolute;
  top: 50%;
  left: 50%;
  margin-top: -25rpx;
  margin-left: -25rpx;

  /* border-radius: 100rpx; */
  /* background: #E9E8EF; */
}

.content-image-title {
  width: 120rpx;
  height: 120rpx;
  border-radius: 100rpx;
  margin-top: -60rpx;
}

/* 行程日历表样式 */
.calendar-section {
  /* width: 90%; */
  /* margin: 80rpx auto; */
  border-radius: 16rpx;
  /* padding: 20rpx; */
  /* box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1); */
  position: relative;
  overflow: hidden;
  /* background: rgba(255, 255, 255, 0.1); */
  /* backdrop-filter: blur(10rpx); */
  /* background: #E9E8EF; */
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  /* opacity: 0.5; */
}

.star-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
  /* transform: translate(-50%, -50%); */
  background: #0c1117;
  /* opacity: 0.5; */
}

.calendar-content {
  margin: 5rpx;
  position: relative;
}

.star {
  position: absolute;
  width: 8rpx;
  height: 8rpx;
  background-color: rgb(246, 240, 240);
  animation: twinkle 3s infinite alternate;
  /* 添加阴影使星星更立体 */
  box-shadow: 0 0 2rpx rgba(255, 255, 255, 0.8);
  /* 使用 transform 来居中星星 */
  transform: translate(-50%, -50%);
  /* 创建五角星形状 */
  clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
}

@keyframes twinkle {
  0% {
    opacity: 0.2;
  }

  100% {
    opacity: 1;
  }
}

.calendar-grid {
  position: relative;
  z-index: 2;
}

.calendar-header {
  margin: 40rpx auto;
  /* text-align: center; */
  /* margin-bottom: 20rpx; */
  position: relative;
  z-index: 2;
}

.date-selector {
  display: inline-block;
  padding: 10rpx 20rpx;
  background: rgba(245, 245, 245, 0.8);
  color: #333333;
  border-radius: 8rpx;
  font-size: 28rpx;
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(221, 221, 221, 0.3);
}

.weekdays {
  display: flex;
  justify-content: space-between;
  padding: 10rpx 0;
  border-bottom: 2rpx solid rgba(255, 255, 255, 0.3);
  position: relative;
  z-index: 2;
}

.weekday {
  flex: 1;
  text-align: center;
  font-size: 24rpx;
  color: #06f461;
}

.days-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-start;
  position: relative;
  z-index: 2;
}

.day-item {
  width: calc(100% / 7);
  aspect-ratio: 1/1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  margin: 5rpx 0;
  perspective: 1000rpx;
  /* 为3D效果准备 */
}

.day-number {
  font-size: 28rpx;
  color: #06f461;
  z-index: 2;
  transition: all 0.3s ease;
}

.today .day-number {
  color: red;
  font-weight: bold;
}

.event-day {
  transform-style: preserve-3d;
  transition: transform 0.5s ease;
}

.event-day .day-number {
  color: #020202;
  background: rgba(245, 245, 245, 0.8);
  border-radius: 50%;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2;
  box-shadow: 0 4rpx 10rpx rgba(0, 0, 0, 0.1);
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(221, 221, 221, 0.3);
}

.event-day:active {
  transform: rotateY(180deg);
}

.selected .day-number {
  background: #0c1117;
  color: #E9E8EF;
  border-radius: 50%;
}

.event-indicator {
  position: absolute;
  bottom: 5rpx;
  width: 6rpx;
  height: 6rpx;
  background: #333333;
  border-radius: 50%;
  z-index: 2;
}

.event-summary {
  /* position: absolute; */
  /* bottom: -30rpx; */
  font-size: 18rpx;
  color: #666666;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  width: 100%;
  text-align: center;
  z-index: 2;
}

/* 活动详情弹窗样式 */
.event-detail-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7);
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.event-detail-modal.show {
  opacity: 1;
  visibility: visible;
}

.event-detail-content {
  width: 80%;
  max-width: 500rpx;
  background: linear-gradient(135deg, #E9E8EF 0%, #f5f5f5 100%);
  border-radius: 20rpx;
  padding: 30rpx;
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.1);
  transform: translateY(50rpx);
  transition: transform 0.3s ease;
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(221, 221, 221, 0.3);
}

.event-detail-modal.show .event-detail-content {
  transform: translateY(0);
}

.event-detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
  padding-bottom: 15rpx;
  border-bottom: 2rpx solid rgba(255, 255, 255, 0.3);
}

.event-detail-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333333;
}

.close-btn {
  width: 40rpx;
  height: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  color: white;
  cursor: pointer;
}

.event-detail-body {
  display: flex;
  flex-direction: column;
  gap: 15rpx;
}

.event-detail-date {
  font-size: 24rpx;
  color: #666666;
  font-weight: 500;
}

.event-detail-description {
  font-size: 26rpx;
  color: #333333;
  line-height: 1.5;
}

.calendar-spacer {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 15rpx;
  height: 100rpx;
}

.calendar-image {
  width: 100rpx;
  height: auto;
  border-radius: 10rpx;
}

/* 响应式设计 - 针对不同屏幕尺寸优化 */
@media (max-width: 768px) {
  .calendar-section {
    width: 90%;
    margin: 20rpx auto;
    /* padding: 15rpx; */
  }

  .date-selector {
    /* padding: 8rpx 16rpx; */
    font-size: 26rpx;
  }

  .weekday {
    font-size: 22rpx;
  }

  .day-number {
    font-size: 26rpx;
  }

  .event-day .day-number {
    width: 50rpx;
    height: 50rpx;
    font-size: 24rpx;
  }

  .selected .day-number {
    width: 50rpx;
    height: 50rpx;
    font-size: 24rpx;
  }
}

@media (max-width: 480px) {
  .calendar-section {
    width: 90%;
    margin: 10rpx auto;
    /* padding: 10rpx; */
  }

  .date-selector {
    /* padding: 6rpx 12rpx; */
    font-size: 24rpx;
  }

  .weekday {
    font-size: 20rpx;
  }

  .day-number {
    font-size: 24rpx;
  }

  .event-day .day-number {
    width: 40rpx;
    height: 40rpx;
    font-size: 20rpx;
  }

  .selected .day-number {
    width: 40rpx;
    height: 40rpx;
    font-size: 20rpx;
  }

  .event-summary {
    font-size: 16rpx;
    bottom: -25px;
  }
}