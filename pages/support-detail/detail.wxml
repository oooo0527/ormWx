<view class="container">
  <view class="header">
    <text class="page-title">{{supportDetail.title}}</text>
  </view>
  
  <view class="detail-content">
    <image class="cover-image" src="{{supportDetail.coverImage}}" mode="aspectFill"></image>
    
    <view class="info-section">
      <view class="info-item">
        <text class="info-label">时间：</text>
        <text class="info-value">{{supportDetail.year}}年{{supportDetail.date}}月</text>
      </view>
      <view class="info-item">
        <text class="info-label">地点：</text>
        <text class="info-value">{{supportDetail.location}}</text>
      </view>
    </view>
    
    <view class="description-section">
      <text class="section-title">活动描述</text>
      <text class="description-text">{{supportDetail.description}}</text>
    </view>
    
    <view class="images-section">
      <text class="section-title">活动图片</text>
      <view class="images-grid">
        <view class="image-item" wx:for="{{supportDetail.images}}" wx:key="index" bindtap="previewImage" data-url="{{item}}">
          <image class="image-thumb" src="{{item}}" mode="aspectFill"></image>
        </view>
      </view>
    </view>
  </view>
</view>