<!-- pages/workRecommend/workRecommend.wxml -->
<view class="container">

  
  <!-- 上部区域 -->
  <view class="upper-section">
    <!-- 海报区域 -->
    <view class="poster-container">
      <image class="main-poster" src="cloud://cloud1-5gzybpqcd24b2b58.636c-cloud1-5gzybpqcd24b2b58-1387507403/ormmm/陈奥/2246a8c4f6c263a32bfbb898a3992cc1.jpg" mode="aspectFill"></image>
      <view class="poster-overlay">
        <text class="poster-title">Orm作品集</text>
        <text class="poster-subtitle">探索她的精彩演艺之路</text>
      </view>
    </view>
    
    <!-- 三搭入口 -->
    <view class="entrance-container">
      <view class="entrance-item movie" bindtap="onEntranceTap" data-type="movie">
        <text class="entrance-icon">🎬</text>
        <text class="entrance-text">电影</text>
      </view>
      <view class="entrance-item tv" bindtap="onEntranceTap" data-type="tv">
        <text class="entrance-icon">📺</text>
        <text class="entrance-text">电视剧</text>
      </view>
      <view class="entrance-item publicity" bindtap="onEntranceTap" data-type="publicity">
        <text class="entrance-icon">❤️</text>
        <text class="entrance-text">公益活动</text>
      </view>
    </view>
  </view>
  
  <!-- 斜着的横幅分割 -->
  <view class="banner-divider">
    <text class="divider-text">作品展示</text>
  </view>
  
  <!-- 下部区域 - 作品轮播图 -->
  <view class="lower-section">
    <!-- 多重叠加轮播图 -->
    <view class="multi-carousel-container">
      <swiper 
        class="multi-carousel" 
        indicator-dots="false" 
        autoplay="false" 
        circular="true" 
        bindchange="onCarouselChange" 
        current="{{currentCarousel}}"
        previous-margin="100rpx"
        next-margin="100rpx"
        snap-to-edge="true"
        display-multiple-items="5">
        <block wx:for="{{works}}" wx:key="id">
          <swiper-item class="carousel-item">
            <view class="work-card {{index === currentCarousel ? 'active' : ''}}" data-index="{{index}}" bindtap="onCardTap">
              <image class="work-cover" src="{{item.cover}}" mode="aspectFill"></image>
              <view class="work-info">
                <text class="work-title">{{item.title}}</text>
                <text class="work-role">饰演: {{item.role}}</text>
                <text class="work-year">{{item.year}}</text>
              </view>
            </view>
          </swiper-item>
        </block>
      </swiper>
      
      <!-- 装饰性背景元素 -->
      <view class="decorative-elements">
        <view class="element circle"></view>
        <view class="element triangle"></view>
        <view class="element square"></view>
      </view>
    </view>
    
    <!-- 作品详情 -->
    <view class="work-details">
      <view class="details-header">
        <text class="details-title">{{works[currentCarousel].title}}</text>
        <view class="type-tag {{works[currentCarousel].type === '电影' ? 'movie-tag' : works[currentCarousel].type === '电视剧' ? 'tv-tag' : 'publicity-tag'}}">
          {{works[currentCarousel].type}}
        </view>
      </view>
      <text class="details-role">饰演: {{works[currentCarousel].role}}</text>
      <text class="details-year">{{works[currentCarousel].year}}</text>
      <text class="details-description">{{works[currentCarousel].description}}</text>
      
      <!-- 交互按钮 -->
      <view class="action-buttons">
        <button class="action-btn like-btn" bindtap="onLikeTap">
          <text class="btn-icon">👍</text>
          <text class="btn-text">点赞</text>
        </button>
        <button class="action-btn share-btn" bindtap="onShareTap">
          <text class="btn-icon">↗️</text>
          <text class="btn-text">分享</text>
        </button>
      </view>
    </view>
  </view>
</view>