.container {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-sizing: border-box;
}



/* Tab 内容区域 */
.tab-content {
  flex: 1;
  /* padding: 20rpx; */
  overflow-y: auto;
  margin-top: 20rpx;
}

/* 卡片菜单容器 */
.card-menu-container {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  /* padding: 10rpx; */
}

/* 卡片项 */
.card-item {
  background: rgba(255, 255, 255, 0.9);
  /* border-radius: 20rpx; */
  padding: 30rpx;
  display: flex;
  flex-direction: column;
  /* align-items: center; */
  justify-content: center;
  text-align: center;
  /* box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.1); */
  backdrop-filter: blur(10px);
  transition: all 0.3s ease;
  position: relative;
  width: 90%;
  margin: 20rpx 0;
  position: relative;
  box-shadow:
    0 8rpx 16rpx rgb(106, 106, 106),
    inset 0 2rpx 4rpx rgb(251, 2, 2),
    inset 0 -2rpx 4rpx rgb(63, 63, 63);
  /* overflow: hidden; */
}

.card-empty {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  color: #f73434;
}

/* 左侧卡片 */
.card-left {
  align-self: flex-start;
  margin-left: 10%;
  border-bottom-left-radius: 20rpx;
  border-top-left-radius: 20rpx;
  align-items: flex-end;
  text-align: right;
  /* margin-right: 5%; */
}

.card-image {
  position: absolute;
  right: 0;
  top: -50rpx;
  width: 96rpx;
  height: 128rpx;

}

.card-image-left {
  position: absolute;
  left: 0;
  /* right: 0; */
  top: -50rpx;
  width: 96rpx;
  height: 128rpx;


}


/* 右侧卡片 */
.card-right {
  align-self: flex-end;
  margin-right: 10%;
  border-bottom-right-radius: 20rpx;
  border-top-right-radius: 20rpx;
  align-items: flex-start;
  text-align: left;
  /* margin-left: 5%; */
}

.card-item:active {
  transform: scale(0.95);
  background: rgba(245, 245, 245, 0.95);
}

/* 卡片图标 */
.card-icon {
  font-size: 60rpx;
  margin-bottom: 20rpx;
}

/* 卡片标题 */
.card-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 10rpx;
  line-height: 1.3;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  width: 100%;
}

/* 卡片描述 */
.card-desc {
  font-size: 24rpx;
  color: #888;
  margin-bottom: 15rpx;
  line-height: 1.4;
  width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

/* 卡片箭头 */
.card-arrow {
  font-size: 28rpx;
  color: #f28787;
  margin-bottom: 15rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  line-height: 1.4;
  width: 100%;
}

/* 加载容器 */
.loading-container {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 200rpx;
  width: 100%;
}

.loading-text {
  font-size: 28rpx;
  color: #888;
}

/* 时间选择器容器 - 占据屏幕的60%高度 */
.time-selector-container {
  height: 60vh;
  /* 占据屏幕的60% */
  width: 100%;
  padding: 40rpx 30rpx 0;
  box-sizing: border-box;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 时间滚轮容器 */
.time-wheel-container {
  position: relative;
  width: 100%;
  height: 400rpx;
  /* 调整高度以容纳5个项目 */
  overflow: hidden;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 25rpx;
  backdrop-filter: blur(15px);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
}

/* 滚轮遮罩层，制造透视效果 */
.time-wheel-mask {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(to bottom,
      rgba(0, 0, 0, 0.3) 0%,
      rgba(0, 0, 0, 0.1) 30%,
      rgba(0, 0, 0, 0.1) 70%,
      rgba(0, 0, 0, 0.3) 100%);
  pointer-events: none;
  z-index: 2;
}

/* 选中区域指示器 - 现在通过动态样式定位 */
.time-wheel-selected-area {
  position: absolute;
  left: 0;
  width: 100%;
  height: 100rpx;
  /* 选中区域高度 */
  /* 位置将通过动态样式设置 */
  border-top: 2rpx solid rgba(255, 255, 255, 0.3);
  border-bottom: 2rpx solid rgba(255, 255, 255, 0.3);
  pointer-events: none;
  z-index: 1;
  background: rgba(255, 255, 255, 0.15);
  /* 添加半透明背景突出选中区域 */
}

/* 滚轮 */
.time-wheel {
  position: relative;
  width: 100%;
  height: 100%;
  z-index: 0;
}

/* 滚轮项目 */
.time-wheel-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 20rpx;
  height: 100rpx;
  /* 每个项目高度为100rpx，5个总共500rpx */
}

.time-wheel-title {
  font-size: 34rpx;
  font-weight: 500;
  color: #ffffff;
  margin-bottom: 8rpx;
  transition: all 0.3s ease;
}

.time-wheel-date {
  font-size: 24rpx;
  color: rgba(173, 171, 171, 0.6);
  transition: all 0.3s ease;
}

/* 占位符项目 */
.time-wheel-item-placeholder {
  /* 用于滚动空间的占位符，无内容 */
}

/* 选中项样式 */
.time-wheel-item.selected {
  transform: scale(1.1);
  z-index: 3;
}

.time-wheel-item.selected .time-wheel-title {
  color: #f73434;
  font-size: 38rpx;
  font-weight: 600;
}

.time-wheel-item.selected .time-wheel-date {
  color: #666;
  font-size: 26rpx;
}

/* 选中内容区域 */
.selected-content {
  padding: 40rpx 20rpx;
  margin: 20rpx;
  background: rgba(255, 255, 255, 0.85);
  border-radius: 10rpx;
  box-shadow: 0 10rpx 40rpx rgba(0, 0, 0, 0.08);
  backdrop-filter: blur(4px);
  position: static;
  max-height: 30vh;
  overflow-y: auto;
  flex-shrink: 0;
  box-sizing: border-box;
}

.content-header {
  text-align: center;
  margin-bottom: 30rpx;
}

.content-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 10rpx;
}

.content-date {
  font-size: 26rpx;
  color: #888;
}

.content-description {
  font-size: 28rpx;
  color: #555;
  line-height: 1.6;
  margin-bottom: 40rpx;
}

.content-actions {
  text-align: center;
}

.action-button {
  background: #000000;
  color: white;
  border: none;
  border-radius: 10rpx;
  padding: 22rpx 65rpx;
  font-size: 28rpx;
  font-weight: 500;
  box-shadow: 0 6rpx 25rpx rgba(102, 126, 234, 0.4);
  transition: all 0.3s ease;
}

.action-button:active {
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 30rpx rgba(102, 126, 234, 0.5);
}

/* 适配picker-view样式 - 简约高级风格 */
.picker-view-column {
  display: flex !important;
  flex-direction: column !important;
  justify-content: center !important;
}

.picker-item {
  text-align: center;
  padding: 0 20rpx;
  transition: all 0.3s ease;
  opacity: 0.6;
  transform: scale(0.95);
}

.picker-item-selected {
  font-size: 38rpx !important;
  color: #333 !important;
  font-weight: 600 !important;
  opacity: 1 !important;
  transform: scale(1.05) !important;
}

/* 标题列表容器 */
.title-list-container {
  height: 100%;
  padding: 40rpx 30rpx;
}

/* 标题项目 */
.title-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 30rpx;
  margin-bottom: 20rpx;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 20rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.1);
  backdrop-filter: blur(10px);
  transition: all 0.3s ease;
}

.title-item:active {
  transform: scale(0.98);
  background: rgba(245, 245, 245, 0.95);
}

.title-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  flex: 1;
}

.title-date {
  font-size: 24rpx;
  color: #888;
  margin-right: 20rpx;
}

.title-arrow {
  font-size: 32rpx;
  color: #667eea;
  transition: transform 0.3s ease;
}

.title-item:active .title-arrow {
  transform: translateX(5rpx);
}

.bookshelf-container-box {
  height: 100%;
  width: 100%;
  display: flex;
  justify-content: flex-end;
  flex-direction: column;
  position: relative;

}

.bookshelf-background {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.bookshelf-container {

  /* background-color: #f5f5f5; */
  /* margin-top: 200px; */

  height: 35%;

  /* 为底部书籍留出空间 */
}

.bookshelf {
  position: relative;
  /* margin-top: 300rpx; */
  height: 100%;
  /* height: calc(100vh - 200rpx); */
  /* 给底部留出空间 */
  width: 100%;
  /* overflow: hidden; */
  /* margin-top: 40%; */
  /* color: #433c47; */
}

.bookshelf1 {
  /* margin-top: 100rpx; */
  position: relative;
  /* margin-top: 20rpx; */
  height: 100%;
  /* height: calc(100vh - 200rpx); */
  /* 给底部留出空间 */
  width: 100%;
}

.book-stack {
  position: absolute;
  left: 50%;
  bottom: 0;
  height: 400rpx;
  transform: translateX(-50%);
  width: 50%;
  background-color: #fff;
  border-radius: 10rpx;
  box-shadow: 0 30rpx 12rpx rgb(0, 0, 0);
  transition: all 0.4s ease;
  transform-origin: bottom center;
  border: 2rpx solid rgb(184, 183, 183);
}


.book-cover {
  width: 100%;
  height: 300rpx;
  border-radius: 10rpx 10rpx 0 0;
}



/* 抽屉动画相关样式 */
.drawer-animation {
  transition: all 0.4s ease;
}

.drawer-open {
  transform: translateX(-50%) translateY(-40rpx) scale(1.05);
  /* box-shadow: 0 20rpx 30rpx rgba(0, 0, 0, 0.2); */
  z-index: 999 !important;
}

/* 底部书籍展示区域 */
.book-list-container {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 200rpx;
  background: linear-gradient(to top, rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0.8));
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.1);
  z-index: 100;
  padding: 20rpx 0;
}

.book-scroll {
  white-space: nowrap;
  width: 100%;
  height: 100%;
}

.book-item-wrapper {
  display: inline-block;
  width: 120rpx;
  height: 160rpx;
  margin: 0 15rpx;
}

.book-item {
  width: 100%;
  height: 100%;
  background-color: #fff;
  border-radius: 10rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  transform: scale(0.9);
}

/* .book-item.selected {
  transform: scale(1.05) translateY(-20rpx);
  box-shadow: 0 12rpx 20rpx rgba(0, 0, 0, 0.15);
} */

.book-item .book-cover {
  width: 100%;
  height: 120rpx;
  border-radius: 10rpx 10rpx 0 0;
}

.book-item .book-title {
  display: block;
  padding: 5rpx;
  font-size: 20rpx;
  text-align: center;
  color: #333;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 当前选中书籍详情 */
.current-book-detail {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 80%;
  background-color: #fff;
  border-radius: 20rpx;
  box-shadow: 0 20rpx 40rpx rgba(0, 0, 0, 0.15);
  padding: 30rpx;
  text-align: center;
  transition: all 0.3s ease;
}

/* .current-book-detail:active {
  transform: translate(-50%, -50%) scale(0.98);
} */

.current-book-cover {
  width: 100%;
  height: 300rpx;
  border-radius: 15rpx;
  margin-bottom: 20rpx;
}

.book-info {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.current-book-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 10rpx;
}

.view-more {
  font-size: 24rpx;
  color: #999;
}




.book-container {
  position: relative;
  width: 100%;
  height: 100%;
  /* box-shadow: 0 4rpx 12rpx rgb(0, 0, 0); */
  /* border: 2px solid rgb(184, 183, 183); */
  /* box-shadow: inset 0 4rpx 12rpx rgb(0, 0, 0); */
}

/* 主书籍 */
.book {
  width: 100%;
  height: 100%;
  position: relative;
  /* background: #fff; */
  border-radius: 5px 8px 8px 5px;
  transform: rotateY(-5deg);
  transition: transform 0.3s ease;
  display: flex;
}

/* 
.book:hover {
  transform: rotateY(-8deg) translateY(-5px);
} */

/* 书脊（左侧） */
.spine {
  /* position: absolute; */
  /* left: -25px; */
  /* top: 0; */
  width: 35px;
  /* height: 100%; */
  border-radius: 5px 0 0 5px;
  box-shadow: 0 8rpx 8rpx #4d4d4d;
  /* background: #333; */
  transform: rotateY(40deg);
  margin-left: -10px;
}

/* 书脊文字 */
.spine-title {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-90deg);
  color: white;
  font-size: 16px;
  font-weight: bold;
  white-space: nowrap;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
  letter-spacing: 2px;
}

/* 封面 */
.cover {
  flex: 1;
  height: 100%;
  /* padding: 50px 40px; */
  /* background: linear-gradient(45deg,
      #3498db 0%,
      #2ecc71 100%); */
  border-radius: 5px 8px 8px 5px;
  position: relative;
  /* overflow: hidden; */
  /* box-shadow: 0 8rpx 8rpx #525151; */

}

/* 封面图案 */
.cover::before {
  content: '';
  position: absolute;
  top: 20px;
  right: 20px;
  width: 80px;
  height: 80px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
}

.cover::after {
  content: '';
  position: absolute;
  bottom: 20px;
  left: 20px;
  width: 60px;
  height: 60px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
}




/* 装饰线 */
.decorative-line {
  position: absolute;
  left: 0;
  right: 0;
  height: 2px;
  background: rgba(255, 255, 255, 0.3);
}

.decorative-line.top {
  top: 30%;
}

.decorative-line.bottom {
  bottom: 30%;
}

/* 底部阴影 */
.shadow {
  position: absolute;
  width: 100%;
  bottom: -20px;
  left: 20px;
  right: 20px;
  height: 20px;
  background: radial-gradient(ellipse at center,
      rgba(0, 0, 0, 0.2) 0%,
      rgba(0, 0, 0, 0) 70%);
  border-radius: 50%;
  z-index: -1;
}